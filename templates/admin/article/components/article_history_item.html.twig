<tr>
    <th scope="row">{{ article.id }}</th>
    <td>{{ article.title }}</td>
    <td>{{ article.description|default('-') }}</td>
    <td><a href="{{ path('app_admin_article_show', {id: article.id}) }}"><i class="fas fa-eye"></i></a></td>
</tr>