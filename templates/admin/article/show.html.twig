{% extends 'base_admin_content.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-xl-6">
            <h1 class="mt-4">{{ article.title }}</h1>
            {{ article.body|raw }}
        </div>
    </div>

    <hr>

    <div class="row my-4">
        <div class="col-xl-6">
            {# ToDo: поменять на рабочую ссылку после реализации функционала генерации статьи #}
            <a href="{{ path('app_admin_article_repeat', { id: article.id }) }}" class="btn btn-primary">Попробовать еще раз</a>
        </div>
    </div>
{% endblock %}
