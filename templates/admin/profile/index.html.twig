{% extends 'base_admin_content.html.twig' %}

{% block content %}
    <h1 class="mt-4">Профиль</h1>
    <div class="row">

        <div class="col-xl-6">
            {# Сообщение об успешном изменении данных #}
            {% if success %}
                {% include 'components/success.html.twig' with {message: 'Профиль успешно изменен'} %}
            {% endif %}

            <div class="card my-4">
                <div class="card-body">

                    <p>Ваш API токен: asft21sfsvzxvghk1912t1g12m</p>
                    <button class="btn btn-outline-primary">Создать новый</button>
                </div>
            </div>

            {% include 'form/user_form.html.twig' with {
                form: userForm,
                buttonName: 'Изменить'
            } %}
        </div>
    </div>
{% endblock %}
