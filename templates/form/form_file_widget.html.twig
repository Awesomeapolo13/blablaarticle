{% block file_widget %}
    <input
            type="file"
            id="{{ id }}"
            multiple="{{ attr.multiple }}"
            class="{{ attr.class }}"
            name="{{ full_name }}"
            placeholder="{{ attr.placeholder }}"
            {{ (required) ? 'required' : '' }}
    >
{% endblock %}
